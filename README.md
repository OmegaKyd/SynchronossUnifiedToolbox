# SynchronossUnifiedToolbox
Unified toolbox for Synchronoss / Verizon cloud backup returns - decrypts selection.zip, extracts contacts, media, &amp; attachments, and generates searchable HTML chat transcripts + call logs.
